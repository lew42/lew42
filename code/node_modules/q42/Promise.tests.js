var test = require("test42");
var view = require("view42/v8");
var assert = test.assert;
var util = require("util42");
var is = util.is;

var P = function(){
	var resolve;
	var reject;

	var p = new Promise(function(res, rej){
		resolve = res;
		reject = rej;
	});

	p.resolve = resolve;
	p.reject = reject;

	return p;
};

var factory = function(){

	var obj = {
		one: function(){

		}
	};
};

var log = console.log.bind(log);

test("basic", function(){
	var one = P();

	var ret = one.then(log, () => { throw "one failed" });
	console.log(one, ret, one === ret);

	/*
	What's the difference between adding cbs directly to a "base" promise, vs chaining?
	*/
});