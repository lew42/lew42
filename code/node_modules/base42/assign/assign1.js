var assign1 = module.exports = function(){
	var arg;
	// allow multiple objects
	for (var i = 0; i < arguments.length; i++){
		arg = arguments[i];
		// loop over each property
		for (var j in arg){
			// assign it to this
			this[j] = arg[j];
		}
	}
	return this;
};