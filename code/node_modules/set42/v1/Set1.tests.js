var test = require("test42");
var view = require("view42");
var assert = test.assert;
var util = require("util42");
var is = util.is;

var Set = require("./Set1");

test("its just like assign", function(){
	var obj = {
		set: Set().fn
	};

	obj.set({
		prop: 1
	});

	assert(obj.prop === 1);

});
