view.set.assignTo --> adoption ?
view.set({}) --> append

view.append({
	prop: ... // --> append anonymously
		// which sets child.parent reference and initializes
	// then makes parent[name] = child reference, and sets child.name = name;
})

view.append.view(self, view)
	view.parent = self;
	view.initialize();
