var nextID = 0;

var enabled = true;

var counts = {};

var track = module.exports = function(obj){
	obj.id = ++nextID;
	// if (track.app && enabled && ["q1", "logger1", "log"].indexOf(obj.name) === -1){
	// 	counts[obj.name] ? counts[obj.name]++ : (counts[obj.name] = 1);
	// 	enabled = false;
	// 	track.app.log(obj.name + "{" + obj.constructor.name + "-" + obj.id +  "-" + counts[obj.name] + "}");
	// 	enabled = true;
	// }
};

track.total = function(){
	console.log("track total:", nextID);
};